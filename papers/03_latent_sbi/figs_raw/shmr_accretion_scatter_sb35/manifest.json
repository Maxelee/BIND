[
 {
  "file": "cell009_out0.png",
  "cell": 9,
  "heading": "4. The stellar\u2013halo mass relation and its scatter",
  "source_head": [
   "lM = np.log10(M)",
   "lS = np.log10(Mstar + 1.0)                       # (n_sobol, N) log stellar mass",
   "lS_truth = np.log10(truth_ms + 1.0) if truth_ms is not None else None",
   "",
   "def binned(x, y, edges):",
   "    idx = np.digitize(x, edges) - 1"
  ]
 },
 {
  "file": "cell011_out1.png",
  "cell": 11,
  "heading": "5. Scatter budget: feedback vs halo-to-halo",
  "source_head": [
   "var_tot  = r.var()",
   "feedback = r.var(axis=0).mean()                  # within-halo, across the Sobol prior",
   "halo_mu  = r.mean(axis=0)                         # param-averaged residual per halo",
   "halo_var = halo_mu.var()                          # halo-to-halo at fixed mass",
   "",
   "print(f\"sigma_tot     = {np.sqrt(var_tot):.4f} dex\")"
  ]
 },
 {
  "file": "cell013_out1.png",
  "cell": 13,
  "heading": "6. How much of the halo-to-halo scatter is accretion history?",
  "source_head": [
   "if have_history and matched.sum() > 20:",
   "    sel = matched & np.isfinite(halo_mu)",
   "    print(f\"accretion-history regression on {sel.sum()} matched halos\\n\")",
   "    for p in PERCENTS:",
   "        tf = tform[f\"t_form_{p}\"]",
   "        ok = sel & np.isfinite(tf)"
  ]
 },
 {
  "file": "cell015_out0.png",
  "cell": 15,
  "heading": "7. The fiducial SHMR colored by formation time",
  "source_head": [
   "# 4-panel fiducial SHMR colored by formation time t_form_{10,25,50,90}.",
   "if not have_history or matched.sum() < 50:",
   "    print(\"history not available \u2014 run sections 1 & 3 first.\")",
   "else:",
   "    lS_fid = np.log10(fid_ms + 1.0)                     # fiducial stellar mass",
   "    fig, axs = plt.subplots(2, 2, figsize=(11, 9))"
  ]
 },
 {
  "file": "cell017_out1.png",
  "cell": 17,
  "heading": "8. Does feedback widen or narrow the early-vs-late former gap?",
  "source_head": [
   "if not have_history or matched.sum() < 50:",
   "    print(\"history not available \u2014 run sections 1 & 3 first.\")",
   "else:",
   "    # mass-controlled early/late split on t_form_50: regress out the mass trend, then",
   "    # take the sigma-tails of the formation-time residual (cleanly separated populations).",
   "    tf50 = tform[\"t_form_50\"]"
  ]
 }
]