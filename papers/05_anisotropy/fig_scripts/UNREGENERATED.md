# Unregenerated figures — Paper V (anisotropy letter)

None. All 4 figures referenced by `main.tex` (`fig01_maps`, `fig02_decomposition`,
`fig03_crossauto`, `fig04_ejection_profiles`) were successfully regenerated as
standalone scripts under `fig_scripts/` and their placeholders in `figs/`
replaced with vector PDFs. See `fig_scripts/notes_fig*.md` for per-figure
data provenance and styling notes, and the regenerator report for the
reproduced numeric diagnostics.
